//Include built in standard input and output.
#include <stdio.h>

//Calling main function to call print with a string as input
main()
{
    printf("hello, world\n");
}
//Compile with `cc printing.c` then run `./a.out`.